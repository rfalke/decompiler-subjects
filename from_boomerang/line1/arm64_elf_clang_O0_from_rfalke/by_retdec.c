//
// This file was generated by the Retargetable Decompiler
// Website: https://retdec.com
//

#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// ------------------------ Structures ------------------------

struct _IO_FILE {
    int32_t e0;
};

// ------------------- Function Prototypes --------------------

char * chomp(char * s, int32_t size, struct _IO_FILE * f);

// ------------------------ Functions -------------------------

// From module:   /mnt/from_boomerang/./line1/source.c
// Address range: 0x400720 - 0x400788
// Line range:    3 - 14
char * chomp(char * s, int32_t size, struct _IO_FILE * f) {
    char * str = fgets(s, size, f); // 0x400744
    if (str == NULL) {
        // 0x400778
        return str;
    }
    char * found_char_pos = strchr(str, 10); // 0x40075c
    if (found_char_pos != NULL) {
        // 0x40076c
        *found_char_pos = 0;
    }
    // 0x400778
    return str;
}

// From module:   /mnt/from_boomerang/./line1/source.c
// Address range: 0x400788 - 0x400830
// Line range:    17 - 32
int main(int argc, char ** argv) {
    char line[1]; // bp-1072, 0x400788
    // 0x400788
    if (argc <= 1) {
        // 0x40081c
        return 1;
    }
    int64_t file_path = *(int64_t *)((int64_t)argv + 8); // 0x4007c8
    struct _IO_FILE * file = fopen((char *)file_path, (char *)0x4008c0); // 0x4007cc
    if (file == NULL) {
        // 0x40081c
        return 1;
    }
    // 0x4007e8
    if (chomp(line, 1024, file) != NULL) {
        // 0x4007fc
        printf("%s\n", line);
    }
    // 0x400810
    fclose(file);
    // 0x40081c
    return 0;
}

// --------------- Dynamically Linked Functions ---------------

// int fclose(FILE * stream);
// char * fgets(char * restrict s, int n, FILE * restrict stream);
// FILE * fopen(const char * restrict filename, const char * restrict modes);
// int printf(const char * restrict format, ...);
// char * strchr(char * s, int c);

// --------------------- Meta-Information ---------------------

// Detected compiler/packer: llvm (3.8.1)
// Detected functions: 2

