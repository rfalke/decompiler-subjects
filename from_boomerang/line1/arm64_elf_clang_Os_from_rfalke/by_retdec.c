//
// This file was generated by the Retargetable Decompiler
// Website: https://retdec.com
// Copyright (c) Retargetable Decompiler <info@retdec.com>
//

#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// ------------------------ Structures ------------------------

struct _IO_FILE {
    int32_t e0;
};

// ------------------- Function Prototypes --------------------

char * chomp(char * s, int32_t size, struct _IO_FILE * f);

// ------------------------ Functions -------------------------

// From module:   /mnt/from_boomerang/./line1/source.c
// Address range: 0x400720 - 0x40075c
// Line range:    3 - 14
char * chomp(char * s, int32_t size, struct _IO_FILE * f) {
    char * str = fgets(s, size, f); // 0x40072c
    if (str == NULL) {
        // 0x40074c
        return str;
    }
    char * found_char_pos = strchr(str, 10); // 0x400740
    if (found_char_pos != NULL) {
        // 0x400748
        *found_char_pos = 0;
    }
    // 0x40074c
    return str;
}

// From module:   /mnt/from_boomerang/./line1/source.c
// Address range: 0x40075c - 0x4007cc
// Line range:    17 - 32
int main(int argc, char ** argv) {
    // 0x40075c
    if (argc < 2) {
        // 0x4007bc
        return 1;
    }
    int64_t file_path = *(int64_t *)((int64_t)argv + 8); // 0x400774
    struct _IO_FILE * file = fopen((char *)file_path, (char *)0x400860); // 0x400780
    if (file == NULL) {
        // 0x4007bc
        return 1;
    }
    // 0x40078c
    int64_t str; // bp-1048, 0x40075c
    if (chomp((char *)&str, 1024, file) != NULL) {
        // 0x4007a0
        puts((char *)&str);
    }
    // 0x4007a8
    fclose(file);
    // 0x4007bc
    return 0;
}

// --------------- Dynamically Linked Functions ---------------

// int fclose(FILE * stream);
// char * fgets(char * restrict s, int n, FILE * restrict stream);
// FILE * fopen(const char * restrict filename, const char * restrict modes);
// int puts(const char * s);
// char * strchr(char * s, int c);

// --------------------- Meta-Information ---------------------

// Detected compiler/packer: llvm (3.8.1)
// Detected functions: 2

