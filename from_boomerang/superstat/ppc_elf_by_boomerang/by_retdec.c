//
// This file was generated by the Retargetable Decompiler
// Website: https://retdec.com
//

#include <stdint.h>
#include <stdio.h>
#include <sys/stat.h>

// ------------------------ Functions -------------------------

// Address range: 0x10000440 - 0x100005bc
int main(int argc, char ** argv) {
    // 0x10000440
    int32_t buf; // bp-128, 0x10000440
    int32_t v1 = stat(*(int32_t *)((int32_t)argv + 4), &buf); // 0x10000470
    printf("res: %i\n", v1);
    printf("dev: %i\n", v1);
    int32_t v2; // 0x10000440
    printf("ino: %i\n", v2);
    printf("mode: %i\n", v2);
    printf("nlink: %i\n", v2);
    printf("uid: %i\n", v2);
    printf("gid: %i\n", v2);
    printf("rdev: %i\n", v2);
    printf("size: %i\n", v2);
    printf("blksize: %i\n", v2);
    printf("blocks: %i\n", v2);
    printf("atime: %i\n", v2);
    printf("mtime: %i\n", v2);
    printf("ctime: %i\n", v2);
    return 0;
}

// --------------- Statically Linked Functions ----------------

// int32_t stat(int32_t a1, int32_t * a2);

// --------------- Dynamically Linked Functions ---------------

// int __xstat(int ver, const char * filename, struct stat * stat_buf);
// int printf(const char * restrict format, ...);

// --------------------- Meta-Information ---------------------

// Detected compiler/packer: gcc (3.3.3)
// Detected functions: 1

