//
// This file was generated by the Retargetable Decompiler
// Website: https://retdec.com
// Copyright (c) Retargetable Decompiler <info@retdec.com>
//

#include <stdint.h>
#include <stdio.h>
#include <sys/stat.h>

// ------------------------ Functions -------------------------

// From module:   /mnt/from_boomerang/./superstat/source.c
// Address range: 0x400640 - 0x4007d4
// Line range:    3 - 21
int main(int argc, char ** argv) {
    // 0x400640
    int64_t st; // bp-152, 0x400640
    printf("res: %i\n", stat(*(int64_t *)((int64_t)argv + 8), &st) & 0xffffffff);
    printf("dev: %i\n", st);
    int64_t v1; // 0x400640
    printf("ino: %i\n", v1);
    int32_t v2; // 0x400640
    printf("mode: %i\n", (int64_t)v2);
    printf("nlink: %i\n", (int64_t)v2);
    printf("uid: %i\n", (int64_t)v2);
    printf("gid: %i\n", (int64_t)v2);
    printf("rdev: %i\n", v1);
    printf("size: %i\n", v1);
    printf("blksize: %i\n", (int64_t)v2);
    printf("blocks: %i\n", v1);
    printf("atime: %i\n", v1);
    printf("mtime: %i\n", v1);
    printf("ctime: %i\n", v1);
    return 0;
}

// --------------- Statically Linked Functions ----------------

// int64_t stat(int64_t a1, int64_t * a2);

// --------------- Dynamically Linked Functions ---------------

// int __xstat(int ver, const char * filename, struct stat * stat_buf);
// int printf(const char * restrict format, ...);

// --------------------- Meta-Information ---------------------

// Detected compiler/packer: llvm (3.8.1)
// Detected functions: 1

