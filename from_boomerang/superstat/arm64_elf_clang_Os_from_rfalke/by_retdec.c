//
// This file was generated by the Retargetable Decompiler
// Website: https://retdec.com
// Copyright (c) Retargetable Decompiler <info@retdec.com>
//

#include <stdint.h>
#include <stdio.h>
#include <sys/stat.h>

// ------------------------ Functions -------------------------

// From module:   /mnt/from_boomerang/./superstat/source.c
// Address range: 0x400640 - 0x400750
// Line range:    3 - 21
int main(int argc, char ** argv) {
    // 0x400640
    int64_t buf; // bp-152, 0x400640
    int64_t v1 = stat(*(int64_t *)((int64_t)argv + 8), &buf); // 0x400658
    printf("res: %i\n", v1 & 0xffffffff);
    printf("dev: %i\n", buf);
    int64_t v2; // 0x400640
    printf("ino: %i\n", v2);
    int32_t v3; // 0x400640
    printf("mode: %i\n", (int64_t)v3);
    printf("nlink: %i\n", (int64_t)v3);
    printf("uid: %i\n", (int64_t)v3);
    printf("gid: %i\n", (int64_t)v3);
    printf("rdev: %i\n", v2);
    printf("size: %i\n", v2);
    printf("blksize: %i\n", (int64_t)v3);
    printf("blocks: %i\n", v2);
    printf("atime: %i\n", v2);
    printf("mtime: %i\n", v2);
    printf("ctime: %i\n", v2);
    return 0;
}

// --------------- Statically Linked Functions ----------------

// int64_t stat(int64_t a1, int64_t * a2);

// --------------- Dynamically Linked Functions ---------------

// int __xstat(int ver, const char * filename, struct stat * stat_buf);
// int printf(const char * restrict format, ...);

// --------------------- Meta-Information ---------------------

// Detected compiler/packer: llvm (3.8.1)
// Detected functions: 1

