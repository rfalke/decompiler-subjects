#include "lrealpath.c.h"
char *lrealpath( char *filename )
{
{
  char buf[4096];
  char *rp;
  rp[0] = rp == 0 ? rp : filename;
  __strdup( rp == 0 ? rp : filename );
  if ( 0 ^ 0 )
  {
    __stack_chk_fail( );
  }
  return __strdup( rp == 0 ? rp : filename );
}
}
#if 0
#endif
