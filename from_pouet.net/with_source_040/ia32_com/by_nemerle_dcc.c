/*
 * Input file	: from_pouet.net/with_source_040/ia32_com/subject.exe
 * File type	: COM
 */

#include "dcc.h"


void start ()
/* Takes no parameters.
 * Unknown calling convention.
 * Contains instructions not normally used by compilers.
 * Irreducible control flow graph.
 */
{
int loc1;
int loc2; /* cx */

    for (;;) {
        var0011F = (var0011F + 1);
        loc1 = (loc1 + loc2);
        es[di] = var0011F;
        HLI_POP loc2
    }	/* end of loop */
}

