/*
 * Input file	: from_pouet.net/with_source_054/ia32_com/subject.exe
 * File type	: COM
 */

#include "dcc.h"


void start ()
/* Takes no parameters.
 * Unknown calling convention.
 * Contains instructions not normally used by compilers.
 * Irreducible control flow graph.
 */
{
int loc1; /* dx */
int loc2; /* bx */
int loc3; /* cx */
int loc4; /* cl */

    do {
        loc1 = 968;
    } while (//*failed*//);

    for (;;) {
        loc2 = (loc2 + loc3);
        *loc2 = loc4;
        loc3 = (loc3 + 18);
    }	/* end of loop */
}

