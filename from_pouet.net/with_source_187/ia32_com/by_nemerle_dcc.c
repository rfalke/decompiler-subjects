/*
 * Input file	: from_pouet.net/with_source_187/ia32_com/subject.exe
 * File type	: COM
 */

#include "dcc.h"


void start ()
/* Takes no parameters.
 * Unknown calling convention.
 * Contains instructions not normally used by compilers.
 * Contains coprocessor instructions.
 */
{
int loc1; /* ax */
int loc2; /* dx */
int loc3; /* al */
int loc4; /* bx */
int loc5; /* cx */
    loc1 = 19;
    loc2 = 968;
    loc3 = 0;

    do {
        loc3 = (loc3 + 1);
    } while (//*failed*//);
    loc2 = 0;

    do {
        loc4 = 0;

        do {
            loc5 = 63;

            do {
            } while (//*failed*//);
            loc4 = (loc4 + 1);
        } while ((loc4 < 320));
        loc2 = (loc2 + 1);
    } while ((loc2 < 200));
}

