/*
#include <stdio.h>

int count_zeros(int length, int *arr) {
    int result=0;
    for(int i=0;i<length;i++) {
        if(arr[i]==0) result++;
    }
    return result;
}
int main() {
    int nums1[ ] = {0,1,2,3};
    int nums2[ ] = {0,1,2,0,3};
    printf("count_zeros: %d\n", count_zeros(4, nums1));
    printf("count_zeros: %d\n", count_zeros(5, nums2));
    return 0;
}
*/
